[phases.setup]
nixPkgs = ["nginx"]

[phases.build]
cmds = [
  "mkdir -p /etc/nginx/conf.d",
  "envsubst '$PORT' < nginx.conf > /etc/nginx/conf.d/default.conf",
  "nginx -t"
]

[start]
cmd = "envsubst '$PORT' < nginx.conf > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
